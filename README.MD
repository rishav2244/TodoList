To run this, go to SpringBackend/TrackerBackend and using that working directory,
run ./gradlew bootRun if on Linux.

Then go to task5 and with it as working directory on a separate console, run
npm run dev.

Go to localhost:5173 and use the app.
